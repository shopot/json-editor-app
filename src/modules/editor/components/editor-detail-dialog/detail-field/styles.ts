import styled from 'styled-components';

import { Colors } from '@/core/components';

export const StyledDetailField = styled.div`
  display: contents;

  span:first-of-type {
    color: ${Colors.Gray600};
  }
`;
